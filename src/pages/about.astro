---
import Layout from "../layouts/Layout.astro";

const base = import.meta.env.BASE_URL || "/";
const H = (p) => `${base}${p.replace(/^\/+/, "")}`;
---
<Layout title="About — Sridhar Rao">
  <h1>About</h1>
  <p>I am a technical writer and developer focused on clear docs, fast sites, and smooth developer experience.</p>
</Layout>

